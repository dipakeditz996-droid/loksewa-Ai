import os; import django; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'backend.settings'); django.setup(); from django.db import connection; cur = connection.cursor(); cur.execute('SELECT tablename FROM pg_tables WHERE schemaname=\'public\';'); tables = [r[0] for r in cur.fetchall()]; print('subscriptions_notification in tables:', 'subscriptions_notification' in tables); print('exams_subject in tables:', 'exams_subject' in tables);
